package com.laerdal.okpi.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // OpenAPI dependency is not present in the current pom.xml.
    // This class reserves the configuration slot from the requested skeleton.
}

