server:
  port: 9083

spring:
  datasource:
    url: jdbc:mysql://localhost:3309/okpi_kpi
    username: root
    password: root

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
